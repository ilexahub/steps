# Changelog

`4.10.0` — обратная связь («Написать разработчику» → сервер / Telegram-личка / страница inbox); публичная витрина этого репозитория.

`4.9.7` — dancer: «Выбрать музыку» над «Вернуть метроном»; Play сворачивает шит к ленте.
`4.9.6` — dancer: спрятана гигантская mp3; имя трека только текст; «Выбрать музыку» второй снизу, каталог только по ней.
`4.9.5` — каталог: один трек на md5 (повторный пост в канал не дублирует список; при старте чистятся старые дубли).
`4.9.4` — dancer: одна «Закрыть» (стиль sheet-close); каталог в отдельном листе со скроллом («Выбрать музыку»), без раздувания основного окна.
`4.9.3` — «Музыка (mp3)»: `display:block` больше не перебивает `hidden` (кнопка реально скрыта у танцора).
`4.9.2` — кнопка «Музыка (mp3)» скрыта у танцора; видна только при активной trainer-сессии (пока каталог тестируется); лог канала каталога явнее (`CATALOG_CHANNEL_ID=`).
`4.9.1` — клавиатура trainer-кода в стиле дока (квадратные плашки 12px, крупные цифры, accent OK); локальный `server.py` читает `.env` (последний дубликат ключа побеждает); хэш кода в env-safe формате с `;`.
`4.9.0` — mp3 разделён на trainer (двойной тап → код 3×4 → движки/фаза) и dancer («Музыка (mp3)» под пресетами, BPM read-only, каталог по темпу); `POST /api/track-cache` только для trainer; ID3-теги в шите; каталог `catalog_tracks` + прокси аудио; индексация Telegram-канала тем же ботом.
`4.8.1` — арбитраж октавы: Essentia/Percival на сальсе перепроверяются кандидатами ×1.5/×2 по onset-сетке.
`4.8.0` — анализ BPM вынесен в Web Worker.
`4.7.2` — uniques в `/stats` в SQL; индекс track_cache; таймер гасится при конце трека.
`4.7.1` — ревизия стабильности WebView, track-cache, cookies, rate-limit, compress.
`4.7.0` — диалоги aria-modal; патч ленты; STATS cookie; health; тесты.
`4.6.0` — точность BPM (44.1 кГц), окно 100–220; WAL SQLite; rate-limit.
`4.5.0` — mp3 track UX: BPM из имени / вручную; busy + таймаут.
`4.4.0` — +31 цветовая схема (всего 38).
`4.3.6` — amvera.yaml + параллельный деплой.
`4.3.5` — шкала текста в настройках; плашки без бокового inset.
`4.3.4` — `--content-max` = ширина дока.
`4.3.3` — kick+turn раздельно; «Добавить Базовый между».
`4.3.2` — iconFrame square default; плашки по центру; Кик→Поворот.
`4.3.1` — track sheet polish.
`4.3.0` — track sheet: дробный BPM; 4 алгоритма.
`4.2.6` — CSP unsafe-eval для Essentia WASM.
`4.2.5` — плавный цвет текста плашек.
`4.2.4` — док до 150px; принудительная Essentia.
`4.2.3` — dock max 150px; unlockAudio timeout.
`4.2.2` — fix метронома в браузере (AudioContext).
`4.2.1` — Essentia Vector fix; ±BPM; скрытый вход в mp3.
`4.2.0` — track sheet UX; кэш фазы/engine.
`4.1.4` — harden compress middleware.
`4.1.3` — fix MIME HTML/CSS/JS на Docker slim.
`4.1.2` — fix 500 на `/stats` при gzip/brotli.
`4.1.1` — тема Светлая / Авто / Тёмная.
`4.1.0` — обводка иконок: круг или квадрат.
`4.0.0` — старт явной нумерации: режим трек вместо метронома.
