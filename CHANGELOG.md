# Changelog

<table width="100%">
<tr><td width="100%">

**Android**

- **Важные шаги 1.0.13** — обновлённый ярлык приложения; скачать APK с [Releases](https://github.com/ilexahub/steps/releases/tag/stepslight-1.0.13) или принять обновление в приложении.
- **Важные шаги 1.0.12** — обновление APK с этой витрины; после скачивания браузер не остаётся за диалогом установки.
- **STEPSLight 1.0.8** — Android-оболочка: скачивание APK из [Releases](https://github.com/ilexahub/steps/releases/tag/stepslight-1.0.8). Внутри — тот же тренажёр (метроном, музыка, камера); обновления ставятся из приложения.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**6.5**

- **6.5.7** — «Мои записи» появляется и когда в кэше только музыка, без видео.
- **6.5.6** — каталог музыки качает треки с витрины и облачного диска сразу (кто ответил первым), не дожидаясь основного сервера.
- **6.5.5** — в ВКонтакте мини-приложение открывается само по себе: список музыки и темп берутся с витрины, когда основной сервер недоступен; нижние кнопки старта снова на экране.
- **6.5.4** — на странице статистики можно править ячейку: поле и галочка подтверждения.
- **6.5.3** — редактор таблиц статистики за кодом тренера.
- **6.5.2** — бегунок перемотки на плашке музыки; оболочка Android спрашивает доступ к музыке.
- **6.5.1** — «Не сохранять» надёжно удаляет несохранённую запись при старте.
- **6.5.0** — каталог сначала с общего облачного диска; камера в нижней панели, запись красной кнопкой на кадре, выключение звука метронома на плашке темпа.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**6.4**

- **6.4.1** — в Android-оболочке снова видны настройки вибрации (отклик кнопок и доли 1…8); по умолчанию вибрация выключена. Каталог музыки дополнительно смотрит зеркало на втором сервере, если основной недоступен.
- **6.4.0** — в статистике видно: id, ник, открытия приложения, загрузки музыки с источника и прослушивания дольше 10 секунд (короткие пробы не считаются).

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**6.3**

- **6.3.5** — в Мессенджере «Сохранить» снова одно нажатие: запись уходит как раньше, без перекодирования на сервере.
- **6.3.4** — сохранение записи в мини-приложении Мессенджера снова кладёт файл в «Загрузки» целиком: системный загрузчик больше не принимает ролик за видеопоток и не оставляет нулевой файл.
- **6.3.3** — запись, сохранённая прямо на устройство (браузер, MAX, приложение-оболочка), тоже сообщает свою длительность: она дописывается в файл на телефоне, минуя сервер.
- **6.3.2** — записи с камеры отдаются нормальным MP4: плеер и галерея показывают верную длительность, а не «3 секунды» у минутного ролика. Готовая запись ждёт сохранения на постоянном диске сервера и переживает его обновление — раньше в такой момент загрузчик телефона складывал в «Загрузки» пустой файл. Если ссылка на запись успела устареть, приложение молча загружает её заново.
- **6.3.1** — запись с камеры пишется чётче (битрейт 2,8 Мбит/с вместо 1,8), а в чат Мессенджера уходит вся длина ролика: раньше плеер обрывал его на первых секундах, потому что файл не сообщал свою длительность. «В чат» теперь первая кнопка, свободное место показывается в гигабайтах.
- **6.3.0** — записи с камеры остаются на устройстве, пока вы сами их не удалите: новый список «Мои записи» с датой, длительностью и размером, кнопками «Открыть» и «Удалить». «Записать ещё» больше не стирает предыдущую запись, а память приложения помечается как защищённая от автоочистки — можно спокойно дождаться связи и сохранить.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**6.2**

- **6.2.8** — запись с камеры уходит в чат Мессенджера файлом с сервера, а не ссылкой: длинные записи больше не отваливаются с ошибкой. Если системное сохранение на устройство не открылось, запись отправляется в чат сама.
- **6.2.7** — каталог музыки почти на весь экран; в подготовке треков верхняя полоска счёта не уезжает под статус телефона; на плашке музыки только имя файла; предупреждение «трек не подготовлен» не перекрывает уже играющий трек.
- **6.2.6** — в режиме «плавно» ушедшая плашка короче, следующая раньше видна из‑под превью камеры.
- **6.2.5** — в режиме камеры на четверть экрана лента шагов поднимается под верхнюю панель; в ВК верхняя панель как в Мессенджере; на диске подготовки треков отдельно виден APK оболочки.
- **6.2.4** — синхронизация mp3 между серверами также подтягивает имя файла, название и исполнителя с основного сервера.
- **6.2.3** — синхронизация mp3 быстрее переключается на копирование через браузер, если второй сервер не отвечает; после смены сервера сохранение имени/тегов больше не цепляет чужой трек.
- **6.2.2** — в подготовке треков синхронизация mp3 между серверами с окном прогресса и остановкой; при стриминге музыки видна полоска догрузки файла.
- **6.2.1** — в подготовке треков можно поправить исполнителя и название, не только имя файла; на витрине добавлены скриншоты настройки «Движение плашек».
- **6.2.0** — в инструменте подготовки треков: копирование mp3 между основным сервером и сервером развёртывания (кнопка ←→), догрузка на диск из каталога канала без повторной заливки, список файлов на диске. Для ВКонтакте бот Мессенджера не нужен: музыка копируется с основного сервера.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**6.1**

- **6.1.7** — в каталоге показывается то имя, которое задали в подготовке треков (без лишнего «артист — …» из тегов); в Мессенджере после «Сохранить» убрана лишняя подсказка про диалог.
- **6.1.6** — в каталоге музыки длинный список снова листается пальцем (раньше жест уходил на экран под окном).
- **6.1.5** — в Мессенджере сохранение видео с камеры снова сразу открывает диалог сохранения, без лишнего просмотра файла.
- **6.1.4** — инструмент подготовки треков снова может писать каталог на сервер развёртывания, не только на основной.
- **6.1.3** — треки для ВКонтакте можно подготовить отдельно на сервере развёртывания (зеркало на диске), даже если Мессенджер с площадки недоступен.
- **6.1.2** — стабильнее открытие и закрытие окон настроек, обратной связи и записи.
- **6.1.1** — надёжнее Play и смена музыки; статусы и кнопка Play понятнее; в режиме камеры подсказка «Play — запись».
- **6.1.0** — названия треков в каталоге можно поправить: имя правится в инструменте подготовки треков и сразу обновляется у танцора, без повторной загрузки музыки. Список каталога там выбирается галочкой.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**6.0**

- **6.0.6** — ВКонтакте: видео с камеры сохраняется на всех способах запуска. В приложении ВК открывается системный диалог, а во встроенном браузере ВК (где такого диалога нет вовсе) файл уходит в «Загрузки».
- **6.0.5** — ВКонтакте: сохранение видео с камеры снова готовит файл заранее и открывает системный диалог ВК по свежему нажатию «Сохранить» (раньше загрузка в том же тапе часто срывала диалог).
- **6.0.4** — ВКонтакте: сохранение видео с камеры снова открывает системный диалог ВК (файл готовится на устройстве). Каталог музыки чаще берёт трек с зеркала на сервере — стабильнее воспроизведение.
- **6.0.3** — ВКонтакте: сохранение видео с камеры — файл готовится один раз, системный диалог ВК открывается по следующему нажатию «Сохранить» (раньше прогресс крутился снова и снова без файла).
- **6.0.2** — ВКонтакте: после записи с камеры сохранение снова открывает системный диалог ВК (как раньше).
- **6.0.1** — кнопка «Музыка» снова открывает каталог; политика и условия читаются прямо в приложении; после записи с камеры понятнее, куда сохраняется файл.
- **6.0.0** — приложение стало проще: у танцора остались метроном, лента шагов, каталог музыки и запись с камеры. Подбор темпа и подготовка треков переехали в отдельный инструмент, поэтому из приложения ушёл весь служебный интерфейс и тяжёлая сторонняя библиотека анализа — сборка стала меньше и запускается быстрее. Запись с камеры теперь доступна всем и сохраняется на устройство (в приложении для телефона ещё системное «Поделиться»).

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**5.9**

- **5.9.1** — загрузка крупных треков в зеркало каталога чанками (обход лимита размера запроса на сервере развёртывания).
- **5.9.0** — музыка из каталога может отдаваться с нашего сервера (зеркало файлов), без скачивания через Мессенджер: в ВКонтакте и в браузере трек играет стабильнее, повторное прослушивание по-прежнему с устройства.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**5.8**

- **5.8.0** — ревизия под нагрузкой и полировка режима mp3: музыка отдаётся быстрее и стабильнее при повторной перемотке; долгий подбор темпа теперь можно прервать — выход из панели действительно отменяет анализ; повторный подбор темпа продолжает песню с того же места, а не с начала; после возврата из фона счёт и лента сходятся с прошедшим временем одним шагом (раньше лента прокручивала пропущенные плашки или счёт замирал); аккуратнее уборка временных файлов записи с камеры и отчёты «написать разработчику» с настоящим номером версии.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**5.7**

- **5.7.0** — музыка для всех: на шите метронома «Музыка (mp3)» и каталог в режиме танцора (темп из базы, без ручной правки). Политика и условия открываются страницами в самом приложении — удобнее для ВКонтакте.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**5.6**

- **5.6.1** — быстрее открывается в браузере и в ВКонтакте: скрипт Мессенджера подключается только при запуске из Мессенджера; раньше без КВН он мог задерживать экран на десятки секунд. ВКонтакте: время записи на превью камеры больше не перекрывается крестиком; если приложение открыто без входа в ВКонтакте — предлагаем войти. Подложка/статус-бар ВК под цвет темы; в настройках — политика и условия использования. Пресеты темпа на тёмной теме читаемые (без светлой системной заливки в WebView); сообщение про камеру само скрывается. «Назад» на главном экране — подтверждение «Выйти из приложения?» (Да / Нет).
- **5.6.0** — ВКонтакте: шапка как в Мессенджере (счёт слева, время справа); время записи на превью камеры с зазором до крестика; после «Сохранить» остаётся только системный диалог ВК; в шите метронома «Остановить метроном» (обводка) / «Запустить клик» (заливка).

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**5.5**

- **5.5.10** — каталог: треки уже на устройстве — обводка цветом активной плашки; подпись «как сортируем» справа у кнопки; без точки кэша; «Запустить клик» в шите метронома; цифры счёта вверху не раздуваются в широком браузере.
- **5.5.9** — запись с камеры стартует по Play после паузы, если камеру включили на уже идущей музыке; каталог по умолчанию по имени файла; превью камеры — заливка у активной кнопки; «Сейчас это РАЗ».
- **5.5.8** — таймер REC идёт ровно; если запись не стартовала — заметное предупреждение на экране; надёжнее первый Play с камерой.
- **5.5.7** — кнопки шита с заливкой как «Играть»; камера — контур выкл / заливка вкл.
- **5.5.6** — ширина плашки времени/REC как раньше; без лишнего верхнего зазора у цифр счёта.
- **5.5.5** — ВКонтакте: нормальные имена видео, сохранение файла, легче первый запуск; время/REC у крестика превью камеры.
- **5.5.4** — fix запуска Mini App ВКонтакте (инициализация моста).
- **5.5.3** — fix открытия Mini App ВКонтакте с параметрами запуска.
- **5.5.2** — верхняя строка: счёт слева, время/REC справа (удобнее под системные кнопки ВК).
- **5.5.1** — ВКонтакте: экран не гаснет на Play; «Отправить в чат» только в Мессенджере.
- **5.5.0** — каталог mp3: повторный выбор без сети (кэш на устройстве), стрим с перемоткой, «Сохранить в чат».

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**5.4**

- **5.4.1** — можно один раз отключить счётчик визитов параметром в ссылке (на устройстве запоминается).
- **5.4.0** — платформа ВКонтакте: Mini App (тема, хаптика, сохранение файлов на телефоне).

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**5.3**

- **5.3.1** — fix: квадраты 1…8 в режиме «Цифры счёта вверху» снова видны.
- **5.3.0** — настройка «Цифры счёта вверху»: восемь квадратов 1…8 вместо полосы точек.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**5.2**

- **5.2.6** — танцор: «Скрыть» внизу; SVG-иконки на кнопках; mp3 не теряется при «Вернуть метроном».
- **5.2.5** — надёжнее первая запись с камеры; камера и в режиме танцора; «Скрыть» в BPM-шите.
- **5.2.4** — fullscreen-камера: корректный отступ под полоску Мессенджера.
- **5.2.3** — fullscreen-камера: отступ под узкую полоску клиента; крупнее размер на REC.
- **5.2.2** — сохранение видео сначала на устройство; загрузка крупными кусками (обход лимитов прокси).
- **5.2.1** — форма «Написать разработчику» удобнее на узком экране.
- **5.2.0** — нативная оболочка (APK / iOS): видео в галерею и системное «Поделиться», «не гасить экран», системная «назад».

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**5.1**

- **5.1.2** — страница запасных прокси: заголовок по центру, абзацы по ширине; карточки вида `N proxy host`.
- **5.1.1** — в публичных текстах (бот, страница прокси, UI, README) везде «Мессенджер».
- **5.1.0** — запасные официальные прокси Мессенджера: страница на сервере приложения (открывается в обычном браузере), команда `/proxy` в боте; список можно пополнить ссылками в личку бота, пока клиент ещё работает. Если Мессенджер уже не открывается — нужна заранее сохранённая закладка на эту страницу.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**5.0**

- **5.0.0** — **режим камеры**: фронтальная камера снимает танцующего, звук метронома или mp3 попадает в файл напрямую из приложения (чисто, без микрофона), лента плашек остаётся на экране; превью камеры скрыто / маленькое / во весь экран; Play — начало записи, Стоп — конец; готовое видео — «Сохранить» или «Отправить в чат с ботом» (до 48 МБ). Пока доступно только тренеру — идёт полевой тест.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**4.10**

- **4.10.0** — обратная связь в настройках («Написать разработчику» → форма в приложении + личка в Мессенджере); публичная витрина [ilexahub/steps](https://github.com/ilexahub/steps); деплой на основной и зеркальный сервер развёртывания.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**4.9**

- **4.9.7** — dancer: «Выбрать музыку» над «Вернуть метроном»; Play сворачивает шит к ленте.
- **4.9.6** — dancer: спрятана гигантская mp3; имя трека только текст; «Выбрать музыку» второй снизу, каталог только по ней.
- **4.9.5** — каталог: один трек на md5 (повторный пост в канал не дублирует список; при старте чистятся старые дубли).
- **4.9.4** — dancer: одна «Закрыть» (стиль sheet-close); каталог в отдельном листе со скроллом («Выбрать музыку»), без раздувания основного окна.
- **4.9.3** — «Музыка (mp3)»: `display:block` больше не перебивает `hidden` (кнопка реально скрыта у танцора).
- **4.9.2** — кнопка «Музыка (mp3)» скрыта у танцора; видна только при активной trainer-сессии (пока каталог тестируется); лог канала каталога явнее (`CATALOG_CHANNEL_ID=`).
- **4.9.1** — клавиатура trainer-кода в стиле дока (квадратные плашки 12px, крупные цифры, accent OK); локальный `server.py` читает `.env` (последний дубликат ключа побеждает); хэш кода в env-safe формате с `;`.
- **4.9.0** — mp3 разделён на trainer (код доступа → движки/фаза) и dancer («Музыка (mp3)» под пресетами, BPM read-only, каталог по темпу); кэш анализа только для trainer; ID3-теги в шите; каталог треков + прокси аудио; индексация канала каталога тем же ботом.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**4.8**

- **4.8.1** — арбитраж октавы: Essentia/Percival на сальсе читали 95/127 вместо 190 (окно 100–220 допускает все три); теперь ответ движка перепроверяется кандидатами ×1.5/×2 по onset-сетке (побеждает самый быстрый, удерживающий ≥75 % энергии), а в JS-движках пик быстрой половины окна (≥150 BPM) с score ≥0.7 перебивает медленный. Разбор кейса — `OCTAVE-ARBITRATION.md`.
- **4.8.0** — анализ BPM вынесен в Web Worker (`track-analyze.worker.js`): Essentia WASM и JS-движки больше не морозят UI; общий DSP в `track-dsp.js`, сэмплы летят в воркер transferable-ом, при недоступном Worker — фолбэк на главный поток; сборка печёт хэш воркера в бандл через esbuild `define`.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**4.7**

- **4.7.2** — uniques в `/stats` считаются в SQL (`COUNT(DISTINCT)`), индекс `track_cache(analyzed_at)`, `.dockerignore`, таймер времени гасится при конце трека, пустой time-чип скрывается через `.is-empty`.
- **4.7.1** — ревизия: фикс краша boot на старых WebView (matchMedia `addListener`), отказ от `NaN`/`Infinity` в track-cache (отравляли общий кэш невалидным JSON), `SameSite=Lax` для stats-cookie (Strict давал 401 по ссылке из мессенджера), rate-limit по правому `X-Forwarded-For`, `hmac.compare_digest` в байтах, cap на строки `visit_clients_daily`, кэш сжатых тел в `compress_middleware` (Essentia WASM больше не жмётся на каждый запрос), Escape в поле BPM не закрывает весь шит, шит не закрывается «назад» во время анализа.
- **4.7.0** — шиты и настройки стали настоящими диалогами: `aria-modal`, перенос и возврат фокуса, Tab не уходит из открытой панели; лента при смене настроек патчится, а не пересобирается, `will-change` только во время игры, время в шапке пишет один таймер; `STATS_TOKEN` из query переезжает в HttpOnly-cookie, у `/stats` своя CSP с nonce, `/health` отдаёт статус БД, сборка падает без `vendor/`; тесты настроек и шаффла (34 JS + 21 Python).

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**4.6**

- **4.6.0** — точность BPM: сигнал в Essentia всегда 44.1 кГц (было −8.8% ошибки на 48 кГц), окно темпа 100–220; JS-движки переписаны (огибающая на prefix-sums, нормированный ACF, парабола, поиск фазы по сетке); MD5 по байтам; SQLite в WAL + busy timeout и через `asyncio.to_thread`, ошибки БД → 503, rate-limit и прунинг на записи, тесты бэкенда в `npm test`; `/vendor/*` кэшируется 30 дней (раньше Essentia `.js` уходил в no-store).

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**4.5**

- **4.5.0** — mp3 track UX: BPM из имени / вручную без авто-анализа; busy + этапы + таймаут 2 мин; смена файла.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**4.4**

- **4.4.0** — +31 цветовая схема (всего 38); «Золотой сапфир» = light роял + dark из мёд-навы.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**4.3**

- **4.3.6** — конфиг и чеклист параллельного деплоя на сервер развёртывания.
- **4.3.5** — шкала текста в настройках; плашки без бокового inset, padding 4px.
- **4.3.4** — `--content-max` = ширина дока; topbar/stage/настройки не растягиваются в ленты.
- **4.3.3** — kick+turn раздельно; «Добавить Базовый между»; UI шагов/назад; иконка kick.
- **4.3.2** — track footer 2/3+1/3; iconFrame square default; плашки по центру; Кик→Поворот (`turn`); docs sync.
- **4.3.1** — track sheet polish: выравнивание BPM, статусы у кнопок алгоритмов, Играть, дробь в доке.
- **4.3.0** — track sheet: сразу при выборе mp3; дробный BPM + ± через оригинал; 4 алгоритма; компактный UI.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**4.2**

- **4.2.6** — CSP: `'unsafe-eval'` для инициализации Essentia WASM (иначе сбой на телефоне).
- **4.2.5** — снова плавный цвет текста плашек, в 2× быстрее заливки (меньше вспышки на середине).
- **4.2.4** — док реально растёт до 150px; дискретный цвет текста плашек; принудительная Essentia + текст ошибки.
- **4.2.3** — dock max 150px; Play не зависает после отмены выбора mp3 (таймаут unlockAudio).
- **4.2.2** — fix метронома в браузере: не зависать на 1-м бите при suspended AudioContext.
- **4.2.1** — Essentia Vector fix; «1» на предыдущий удар; ±BPM / сброс; скрытый вход в mp3; док не раздувается в браузере.
- **4.2.0** — track sheet UX (Play, алгоритм), кэш фазы/engine, целое BPM + raw в скобках.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**4.1**

- **4.1.4** — harden compress middleware (MIME, charset, no ETag leak on recompress).
- **4.1.3** — fix MIME HTML/CSS/JS на Docker slim (страница скачивалась вместо открытия).
- **4.1.2** — fix 500 на `/stats` и HTML/JSON при gzip/brotli (Content-Type).
- **4.1.1** — тема: Светлая / Авто / Тёмная (авто = OS / Мессенджер / MAX).
- **4.1.0** — обводка иконок на плашках: круг или скруглённый квадрат (настройка).

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**4.0**

- **4.0.0** — режим **трек вместо метронома** (анализ mp3 на устройстве); с этого релиза — явная нумерация `MAJOR.MINOR.PATCH`.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**3.2** *(ретро)*

- **3.2.4** — кликабельные разбивки по дням на странице статистики (host / OS).
- **3.2.3** — HTML-страница статистики под токеном.
- **3.2.2** — отдельно hits и unique devices (`client_id`).
- **3.2.1** — хранение визитов ~360 дней; пути БД на volume.
- **3.2.0** — анонимные счётчики визитов по host/OS + защищённый stats API.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**3.1** *(ретро)*

- **3.1.1** — док: Play/Pause отдельно от Stop, shuffle в настройки, квадратный док, SVG play/pause, Back на экране цвета.
- **3.1.0** — расширенные настройки последовательности; масштаб хрома под экран.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**3.0** *(ретро)*

- **3.0.3** — minify CSS в prod-сборке.
- **3.0.2** — полировка ленты: пауза/resume, пипсы, типографика дока.
- **3.0.1** — отступ safe-area Мессенджера (без двойного верхнего padding).
- **3.0.0** — главный UX: сетка плашек → **движущаяся лента** через рамку фокуса; пипсы; режимы движения; больше палитр.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**2.6** *(ретро)*

- **2.6.1** — полировка нижней панели и timeline.
- **2.6.0** — меню настроек; схемы Шалфей/Лаванда; BPM по умолчанию 140.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**2.5** *(ретро)*

- **2.5.1** — скрипт палитры вынесен; CSP на странице превью цветов.
- **2.5.0** — безопасность и стабильность: wake lock, lookahead-аудио, хэшированные ассеты, CSP, тесты метронома.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**2.4** *(ретро)*

- **2.4.0** — документация палитры и страница превью цветов.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**2.3** *(ретро)*

- **2.3.0** — бот упрощён до входа в Mini App (`/start` + menu button).

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**2.2** *(ретро)*

- **2.2.1** — смена шага на входящих 5 и 1; локальный Node-сервер для исходников.
- **2.2.0** — плашки шагов двигаются только на акцентах 1 и 5.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**2.1** *(ретро)*

- **2.1.1** — docs под prod-сборку.
- **2.1.0** — Мессенджер + MAX: общий `platform.js`.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**2.0** *(ретро)*

- **2.0.0** — prod-сборка (esbuild → `dist/`), Docker multi-stage, отдаётся только бандл.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**1.3** *(ретро)*

- **1.3.5** — синхронизация docs; шаблон env; хронология в handoff.
- **1.3.4** — иконки звука SVG вместо emoji.
- **1.3.3** — читаемость плашек (размер иконок и подписей).
- **1.3.2** — каталог шагов: «Базовый», default из 6, id/иконки basestep / sonstep / turn.
- **1.3.1** — последовательности от 2 шагов.
- **1.3.0** — точка входа Mini App = `index.html`; прямые ссылки.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**1.2** *(ретро)*

- **1.2.4** — фикс: метроном не останавливался после первого бита.
- **1.2.3** — timeline из 8 маркеров вместо скользящей точки.
- **1.2.2** — HANDOFF + правила для продолжения в новом чате.
- **1.2.1** — диагностика отсутствующего `BOT_TOKEN`.
- **1.2.0** — музыкальный счёт 1…8 отделён от подсветки плашек; акцент на 1 и 5.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**1.1** *(ретро)*

- **1.1.1** — старт на хосте до появления публичного домена.
- **1.1.0** — деплой 24/7: `server.py` + Docker на основной сервер развёртывания.

</td></tr>
</table>

<table width="100%">
<tr><td width="100%">

**1.0** *(ретро)*

- **1.0.0** — первый продукт: Mini App для Мессенджера, метроном, сетка плашек шагов, настройки BPM/последовательности, бот с кнопкой Web App.

</td></tr>
</table>
